import styles from ".//css/Loading.module.css";

function Loading() {
    return (
        <div className={styles.loader}>
          <span>Loading...</span>
        </div>
    )
}

export default Loading;
